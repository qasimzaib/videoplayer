import React from "react";
import { Text, View } from 'react-native';

const VideoPlayer = () => {
	return (
		<View>
			<Text>This is a video Player</Text>
		</View>
	)
}

export default VideoPlayer;